# 数据预处理决策树🌳

> **核心理念**："系统化决策流程，确保预处理完整性和正确性"
>
> **适用场景**：重要项目、生产环境、需要完整文档的项目

---

## 📋 目录

1. [使用指南](#1-使用指南)
2. [10步完整决策流程](#2-10步完整决策流程)
3. [可视化决策树](#3-可视化决策树)
4. [实战案例演示](#4-实战案例演示)
5. [常见决策路径](#5-常见决策路径)

---

## 1. 使用指南

### 1.1 本文档定位

| 特点 | 说明 |
|------|------|
| **覆盖范围** | 完整的预处理决策流程（10个步骤） |
| **使用时间** | 1-2小时（系统化执行） |
| **适用场景** | 重要项目、生产环境、需要文档的项目 |
| **决策深度** | 深度决策，包含所有分支和特殊情况 |

### 1.2 与其他文档的关系

```
preprocessing_quick_reference.md（15分钟速查）
  ↓ 快速不够，需要深入
preprocessing_decision_tree.md（本文档：1-2小时系统化）
  ↓ 需要深入理论
missing_values_strategies.md  ← 缺失值专题
outlier_detection_methods.md  ← 异常值专题
feature_engineering_cookbook.md  ← 特征工程专题
```

### 1.3 使用方式

**模式1：完整执行（推荐）**
- 按10步顺序执行
- 每步记录决策和参数
- 适合：重要项目、首次处理新数据

**模式2：选择性执行**
- 根据数据问题跳转到特定步骤
- 适合：已诊断出具体问题

**模式3：检查验证**
- 已完成预处理，用本流程检查是否遗漏
- 适合：代码审查、质量保证

---

## 2. 10步完整决策流程

### Step 1: 数据概况扫描

**目标**：快速了解数据规模、类型、基本问题

#### 检查清单

- [ ] 数据形状（行数 × 列数）
- [ ] 特征类型分布（数值、分类、时间）
- [ ] 缺失值概况（哪些列、多少比例）
- [ ] 目标变量分布（分类问题的平衡性）
- [ ] 内存占用（是否需要优化）

#### 快速诊断代码

```python
import pandas as pd
import numpy as np

# 基础信息
print(f"数据形状: {df.shape}")
print(f"内存占用: {df.memory_usage(deep=True).sum() / 1024**2:.2f} MB\n")

# 特征类型
print("特征类型分布:")
print(df.dtypes.value_counts())

# 缺失值
print(f"\n缺失值概况:")
missing = df.isnull().sum()
print(missing[missing > 0].sort_values(ascending=False))

# 目标变量（如果是分类问题）
if 'target' in df.columns:
    print(f"\n目标变量分布:")
    print(df['target'].value_counts(normalize=True))
```

#### 决策点

```
数据规模判断：
├─ <1000行 → ⚠️  数据量偏小，注意过拟合
├─ 1000-100K → ✅ 标准处理流程
├─ 100K-1M → ⚠️  注意计算效率
└─ >1M → ⚠️  考虑采样或分布式处理

特征数量判断：
├─ <20列 → 简单场景
├─ 20-100列 → 标准场景
├─ 100-1000列 → 高维数据，需要特征选择
└─ >1000列 → 考虑降维（PCA、自动编码器）
```

---

### Step 2: 缺失值诊断与处理⭐

**目标**：识别缺失值模式，选择合适的处理策略

#### 决策树

```
[发现缺失值]
    │
    ├─ 缺失率 > 50%？
    │   ├─ 是 → 【删除该列】
    │   │       └─ 代码：df.drop(columns=['col'], inplace=True)
    │   │
    │   └─ 否 → 继续
    │
    ├─ 缺失率 < 5%？
    │   ├─ 是 → 【删除缺失行】
    │   │       └─ 代码：df.dropna(subset=['col'], inplace=True)
    │   │       └─ 条件：样本量充足(>10K)
    │   │
    │   └─ 否 → 继续
    │
    ├─ 判断缺失机制
    │   ├─ MCAR（完全随机）
    │   │   └─ 缺失率5-20% → 【均值/中位数/众数填充】
    │   │       └─ 代码位置：data_preprocessing.py:166-187
    │   │
    │   ├─ MAR（随机缺失）
    │   │   └─ 缺失率5-50% → 【KNN填充】或【迭代填充】
    │   │       └─ 代码：SimpleImputer / KNNImputer
    │   │
    │   └─ MNAR（非随机）
    │       └─ 任意比例 → 【建模为特殊类别】
    │           ├─ 数值特征：添加is_missing列
    │           └─ 分类特征：新增"Unknown"类别
    │
    └─ 验证填充效果
        └─ 检查填充后的分布是否合理
```

#### 如何判断缺失机制

| 缺失机制 | 判断方法 | 示例 |
|---------|---------|------|
| **MCAR** | 缺失与所有变量都无关 | 传感器随机故障 |
| **MAR** | 缺失与观测值相关，但与缺失值本身无关 | 年轻人不愿填收入 |
| **MNAR** | 缺失与缺失值本身相关 | 高收入者不填收入 |

**检验方法**：
```python
# Little's MCAR Test（复杂，可选）
# 简单判断：查看缺失值与其他变量的关系
import seaborn as sns
import matplotlib.pyplot as plt

# 缺失值与其他变量的关系
df['income_missing'] = df['income'].isnull().astype(int)
print(df.groupby('income_missing')['age'].mean())

# 如果不同缺失组的其他变量均值差异大 → MAR或MNAR
# 如果差异小 → 可能是MCAR
```

#### 决策矩阵

| 缺失率 | 缺失机制 | 推荐方案 | 代码位置 |
|--------|---------|---------|----------|
| <5% | MCAR | 删除行 | `df.dropna()` |
| 5-20% | MCAR | 均值/中位数/众数 | `data_preprocessing.py:166` |
| 5-20% | MAR | KNN填充 | `KNNImputer` |
| 20-50% | MAR | 迭代填充 | `IterativeImputer` |
| 20-50% | MNAR | 建模为特殊类别 | 手动处理 |
| >50% | 任意 | 删除列 | `df.drop(columns)` |

#### 延伸阅读

详细理论和方法见：[missing_values_strategies.md](missing_values_strategies.md)

---

### Step 3: 异常值检测与处理⭐

**目标**：识别异常值，判断性质，选择处理策略

#### 决策树

```
[发现异常值]
    │
    ├─ 选择检测方法
    │   ├─ 数据量 <10K → IQR方法（快速）
    │   ├─ 数据量 >10K → Isolation Forest（准确）
    │   └─ 高维数据 → LOF或One-Class SVM
    │
    ├─ 判断异常值性质（关键！）
    │   │
    │   ├─ 数据错误？（物理上不可能）
    │   │   └─ 是 → 【删除】或【修正】
    │   │       ├─ 例如：年龄200岁 → 删除
    │   │       └─ 例如：小数点错误 → 修正
    │   │
    │   ├─ 真实极端值？（符合业务逻辑）
    │   │   ├─ 算法类型？
    │   │   │   ├─ 线性模型/SVM → 【鲁棒标准化】或【log变换】
    │   │   │   │   └─ 代码：RobustScaler / np.log1p()
    │   │   │   │
    │   │   │   └─ 树模型 → 【保留】
    │   │   │       └─ 原因：树模型对异常值鲁棒
    │   │   │
    │   │   └─ 例如：富豪收入、豪宅价格
    │   │
    │   └─ 潜在异常事件？（需要关注）
    │       └─ 【单独建模】或【标记】
    │           ├─ 例如：欺诈交易 → 异常检测
    │           └─ 例如：异常行为 → 标记研究
    │
    └─ 验证处理效果
        └─ 绘制处理前后的分布对比
```

#### 异常值检测方法对比

| 方法 | 复杂度 | 适用场景 | 优点 | 缺点 |
|------|--------|---------|------|------|
| **IQR** | 低 | 单变量、快速筛查 | 简单、快速 | 假设单变量 |
| **3σ法** | 低 | 正态分布 | 简单 | 要求正态分布 |
| **Isolation Forest** | 中 | 高维、多变量 | 无监督、准确 | 计算成本中等 |
| **LOF** | 高 | 密度不均 | 适应局部密度 | 计算成本高 |

#### 代码示例

```python
from src.data_preprocessing import handle_outliers_iqr

# 方法1：IQR检测+截断（推荐）
df_clean = handle_outliers_iqr(
    df,
    columns=['income', 'age', 'score'],
    method='clip',  # 截断到上下界
    k=1.5
)

# 方法2：Isolation Forest检测
from sklearn.ensemble import IsolationForest

outlier_detector = IsolationForest(contamination=0.1, random_state=42)
outlier_labels = outlier_detector.fit_predict(df[numeric_cols])

# -1表示异常值，1表示正常值
df['is_outlier'] = (outlier_labels == -1).astype(int)
print(f"检测到 {df['is_outlier'].sum()} 个异常样本")
```

#### 延伸阅读

详细理论和方法见：[outlier_detection_methods.md](outlier_detection_methods.md)

---

### Step 4: 重复值检查

**目标**：识别并处理重复样本

#### 决策树

```
[检查重复值]
    │
    ├─ 完全重复（所有列相同）
    │   └─ 【删除重复行】
    │       └─ 代码：df.drop_duplicates(keep='first')
    │
    ├─ 特征重复但目标不同（数据矛盾）
    │   ├─ 少数样本(< 10个) → 【删除】
    │   └─ 较多样本 → 【深入调查数据质量】
    │
    └─ 关键字段重复（如用户ID重复）
        └─ 【聚合处理】
            ├─ 时间序列：保留最新记录
            ├─ 交易记录：聚合统计
            └─ 用户行为：计数特征
```

#### 代码示例

```python
from src.data_preprocessing import remove_duplicates

# 完全重复检查
print(f"重复行数: {df.duplicated().sum()}")

# 删除重复
df_dedup = remove_duplicates(df, keep='first')

# 指定列检查重复（如用户ID）
df_dedup_user = remove_duplicates(df, subset=['user_id'], keep='last')
```

---

### Step 5: 特征类型分析与转换

**目标**：确保每个特征的类型正确，便于后续处理

#### 决策树

```
[分析特征类型]
    │
    ├─ 数值特征
    │   ├─ 检查分布
    │   │   ├─ 正态分布 → 继续
    │   │   ├─ 右偏（收入、房价） → 【log变换】
    │   │   │   └─ 代码：df['col_log'] = np.log1p(df['col'])
    │   │   └─ 左偏 → 【幂变换】或【Box-Cox】
    │   │
    │   └─ 检查范围
    │       ├─ 有负值 → 无法用log，考虑其他变换
    │       └─ 全为正值 → 可以使用log或Box-Cox
    │
    ├─ 分类特征
    │   ├─ 当前类型为object/string？
    │   │   └─ 是 → 待编码（Step 6处理）
    │   │
    │   ├─ 当前类型为数值但实际是分类？
    │   │   └─ 是 → 【转换为category类型】
    │   │       ├─ 例如：邮编、用户ID
    │   │       └─ 代码：df['col'] = df['col'].astype('category')
    │   │
    │   └─ 检查基数
    │       ├─ 基数 < 10 → 低基数，One-Hot编码
    │       ├─ 基数 10-50 → 中等，Target或Binary编码
    │       └─ 基数 > 50 → 高基数，Target或Frequency编码
    │
    └─ 时间特征
        ├─ 当前为string？
        │   └─ 【转换为datetime】
        │       └─ 代码：pd.to_datetime(df['col'])
        │
        └─ 已是datetime → 【提取特征】（Step 8处理）
```

#### 代码示例

```python
# 1. 检查数值特征分布
numeric_cols = df.select_dtypes(include=[np.number]).columns

for col in numeric_cols:
    skewness = df[col].skew()
    if abs(skewness) > 1:
        print(f"{col}: 偏度={skewness:.2f} → 建议变换")

# 2. log变换（右偏分布）
right_skewed_cols = ['income', 'house_price', 'transaction_amount']
for col in right_skewed_cols:
    if col in df.columns and df[col].min() >= 0:
        df[f'{col}_log'] = np.log1p(df[col])

# 3. 转换分类特征类型
id_cols = ['user_id', 'product_id', 'zipcode']
for col in id_cols:
    if col in df.columns:
        df[col] = df[col].astype('category')

# 4. 转换时间特征
datetime_cols = ['signup_date', 'purchase_date', 'last_login']
for col in datetime_cols:
    if col in df.columns:
        df[col] = pd.to_datetime(df[col])
```

---

### Step 6: 分类特征编码⭐

**目标**：将分类特征转换为数值，便于算法使用

#### 决策树

```
[分类特征编码]
    │
    ├─ 判断特征是否有序
    │   ├─ 有序（如学历：小学<中学<大学）
    │   │   └─ 【Label编码】
    │   │       └─ 代码：LabelEncoder() 或手动映射
    │   │       └─ 适合：树模型、有序关系明确
    │   │
    │   └─ 无序（如城市、性别）
    │       └─ 判断基数
    │
    ├─ 基数 < 10（低基数）
    │   ├─ 算法类型？
    │   │   ├─ 线性模型/NN → 【One-Hot编码】
    │   │   │   └─ 代码：pd.get_dummies() 或 OneHotEncoder()
    │   │   │   └─ 参数：drop_first=True（避免多重共线性）
    │   │   │
    │   │   └─ 树模型 → 【Label编码】或【One-Hot】均可
    │   │       └─ Label编码更紧凑
    │   │
    │   └─ 决策：One-Hot（安全选择）
    │
    ├─ 基数 10-50（中等基数）
    │   ├─ 算法类型？
    │   │   ├─ 树模型 → 【Target编码】⭐
    │   │   │   └─ 用目标变量均值替换类别
    │   │   │   └─ 注意：需要交叉验证避免过拟合
    │   │   │
    │   │   └─ 线性模型 → 【Binary编码】
    │   │       └─ 先Label编码，再转二进制
    │   │       └─ 维度介于One-Hot和Label之间
    │   │
    │   └─ 决策：Target编码（效果好，维度低）
    │
    └─ 基数 > 50（高基数）
        ├─ 【Target编码】+ 【Frequency编码】
        │   └─ Target：用目标变量统计量
        │   └─ Frequency：用出现频率
        │
        └─ 或者【特征哈希】（Hash Encoding）
            └─ 固定维度，可能碰撞
```

#### 编码方法对比

| 编码方法 | 基数要求 | 维度变化 | 适用算法 | 代码位置 |
|---------|---------|---------|---------|----------|
| **One-Hot** | <10 | N→K（K=类别数） | 线性、NN | `data_preprocessing.py:374` |
| **Label** | 任意 | N→N | 树模型 | `data_preprocessing.py:383` |
| **Target** | >10 | N→N | 树模型 | `data_preprocessing.py:299` |
| **Binary** | 10-100 | N→log₂(K) | 任意 | 外部库 |
| **Frequency** | >50 | N→N | 树模型 | 手动计算 |

#### 代码示例

```python
from src.data_preprocessing import encode_categorical_features, CategoricalEncoder

# 方法1：快速One-Hot（低基数）
df_encoded = encode_categorical_features(
    df,
    columns=['gender', 'city', 'product_type'],
    method='onehot',
    drop_first=True
)

# 方法2：Target编码（中高基数）
encoder = CategoricalEncoder(encoding_type='target')
df_encoded = encoder.fit_transform(
    df,
    columns=['user_id', 'product_id'],
    target=df['target']
)

# 方法3：Label编码（树模型）
df_encoded = encode_categorical_features(
    df,
    columns=['education', 'rank'],
    method='label'
)
```

---

### Step 7: 数值特征缩放

**目标**：统一数值特征的尺度，提升算法性能

#### 决策树

```
[数值特征缩放]
    │
    ├─ 算法是否需要缩放？
    │   ├─ 树模型（RF/XGB/LGBM） → ❌ 不需要，跳过
    │   ├─ 朴素贝叶斯 → ❌ 不需要，跳过
    │   │
    │   └─ 线性模型/SVM/NN/KNN/PCA → ✅ 需要
    │
    ├─ 选择缩放方法
    │   ├─ 数据有异常值？
    │   │   ├─ 是 → 【RobustScaler】⭐
    │   │   │   └─ 用中位数和IQR缩放
    │   │   │   └─ 代码：data_preprocessing.py:413
    │   │   │
    │   │   └─ 否 → 继续
    │   │
    │   ├─ 算法类型？
    │   │   ├─ 线性模型/SVM/PCA → 【StandardScaler】
    │   │   │   └─ 转换为均值0、方差1
    │   │   │   └─ 代码：data_preprocessing.py:409
    │   │   │
    │   │   └─ 神经网络 → 【MinMaxScaler】[0,1]
    │   │       └─ 归一化到[0,1]区间
    │   │       └─ 代码：data_preprocessing.py:411
    │   │
    │   └─ 特殊情况
    │       └─ 数据已经在合理范围 → 可以跳过
    │
    └─ 注意事项
        ├─ 训练集fit，测试集transform
        ├─ 保存scaler参数（生产环境需要）
        └─ 只缩放数值列，不缩放分类编码后的0/1列
```

#### 缩放方法对比

| 方法 | 公式 | 结果范围 | 何时使用 | 代码位置 |
|------|------|---------|---------|----------|
| **StandardScaler** | (X-μ)/σ | 均值0，方差1 | 正态分布、线性模型 | `data_preprocessing.py:409` |
| **MinMaxScaler** | (X-min)/(max-min) | [0,1] | 有界数据、神经网络 | `data_preprocessing.py:411` |
| **RobustScaler** | (X-median)/IQR | - | 有异常值 | `data_preprocessing.py:413` |

#### 代码示例

```python
from src.data_preprocessing import FeatureScaler

# 方法1：标准化（最常用）
scaler = FeatureScaler(method='standard')
df_scaled = scaler.fit_transform(df, columns=numeric_cols)

# 方法2：归一化（神经网络）
scaler = FeatureScaler(method='minmax')
df_scaled = scaler.fit_transform(df, columns=numeric_cols)

# 方法3：鲁棒缩放（有异常值）
scaler = FeatureScaler(method='robust')
df_scaled = scaler.fit_transform(df, columns=numeric_cols)

# 训练集/测试集分开处理（重要！）
from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# fit只在训练集
scaler = FeatureScaler(method='standard')
scaler.fit(X_train)

# transform应用到训练集和测试集
X_train_scaled = scaler.transform(X_train)
X_test_scaled = scaler.transform(X_test)
```

---

### Step 8: 特征工程（可选但推荐）⭐

**目标**：创造新特征，提升模型性能

#### 决策树

```
[特征工程]
    │
    ├─ 时间特征（如有datetime列）⭐
    │   └─ 【提取时间成分】
    │       ├─ 年、月、日、星期几
    │       ├─ 是否周末、是否节假日
    │       ├─ 距今天数、季度
    │       └─ 代码：feature_engineering.py:525
    │
    ├─ 交互特征⭐
    │   ├─ 【算术组合】
    │   │   ├─ 乘法：重要特征相乘（如年龄×收入）
    │   │   ├─ 除法：创建比例（如收入/支出）
    │   │   ├─ 加减：创建差值（如收入-支出）
    │   │   └─ 代码：feature_engineering.py:363
    │   │
    │   └─ 【多项式特征】
    │       ├─ X², X³（高次项）
    │       ├─ X₁×X₂（交叉项）
    │       └─ 代码：PolynomialFeatures
    │
    ├─ 聚合特征
    │   └─ 【分组统计】
    │       ├─ 按类别分组，计算统计量
    │       ├─ mean, std, min, max, count
    │       └─ 代码：feature_engineering.py:411
    │
    ├─ 缺失值标记⭐
    │   └─ 【is_missing列】
    │       └─ df['col_missing'] = df['col'].isnull().astype(int)
    │
    └─ 特征选择（可选）
        └─ 【去除冗余特征】
            ├─ 低方差特征
            ├─ 高相关特征（r>0.95）
            └─ 特征重要性排序
```

#### 优先级建议

| 优先级 | 特征类型 | 适用场景 | 价值 |
|--------|---------|---------|------|
| **P0** | 时间特征提取 | 有datetime列 | ⭐⭐⭐ |
| **P0** | 缺失值标记 | 缺失率5-20%的列 | ⭐⭐⭐ |
| **P1** | 交互特征（乘法） | 有核心特征 | ⭐⭐⭐ |
| **P1** | 比例特征 | 有相关数值 | ⭐⭐ |
| **P2** | 聚合特征 | 有分组数据 | ⭐⭐ |
| **P2** | 多项式特征 | 线性模型 | ⭐ |

#### 代码示例

```python
from src.feature_engineering import (
    extract_datetime_features,
    create_interaction_features,
    create_aggregation_features
)

# 1. 时间特征（P0）
if 'signup_date' in df.columns:
    df = extract_datetime_features(df, datetime_column='signup_date')

# 2. 缺失值标记（P0）
for col in ['income', 'credit_score']:
    if df[col].isnull().sum() > 0:
        df[f'{col}_is_missing'] = df[col].isnull().astype(int)

# 3. 交互特征（P1）
df = create_interaction_features(
    df,
    columns=['age', 'income', 'tenure'],
    operations=['*', '/']  # 乘法和除法
)

# 4. 聚合特征（P2，如有分组）
if 'user_id' in df.columns:
    df = create_aggregation_features(
        df,
        group_by='user_id',
        agg_columns=['transaction_amount'],
        agg_functions=['mean', 'std', 'count']
    )
```

#### 延伸阅读

详细理论和方法见：[feature_engineering_cookbook.md](feature_engineering_cookbook.md)

---

### Step 9: 数据平衡（分类问题）

**目标**：处理类别不平衡问题

#### 决策树

```
[检查类别平衡]
    │
    ├─ 计算不平衡比例
    │   └─ ratio = 少数类样本数 / 多数类样本数
    │
    ├─ ratio > 0.8 → ✅ 平衡，无需处理
    │
    ├─ 0.5 < ratio ≤ 0.8 → ⚠️  轻度不平衡
    │   └─ 【类权重调整】
    │       └─ class_weight='balanced'
    │
    ├─ 0.2 < ratio ≤ 0.5 → ⚠️  中度不平衡
    │   ├─ 【SMOTE过采样】⭐
    │   │   └─ 合成少数类样本
    │   └─ 或【类权重+阈值调整】
    │
    └─ ratio ≤ 0.2 → ❌ 严重不平衡
        ├─ 【组合策略】
        │   ├─ SMOTE过采样
        │   ├─ + 随机欠采样
        │   └─ + 类权重调整
        │
        └─ 或【异常检测建模】
            └─ 将少数类视为异常检测问题
```

#### 代码示例

```python
from src.data_preprocessing import balance_data

# 检查平衡性
print(y.value_counts(normalize=True))

# 方法1：SMOTE过采样
X_resampled, y_resampled = balance_data(
    X, y,
    method='smote',
    sampling_strategy='auto',  # 自动平衡
    random_state=42
)

# 方法2：类权重（模型内调整）
from sklearn.ensemble import RandomForestClassifier

clf = RandomForestClassifier(class_weight='balanced')
clf.fit(X_train, y_train)
```

---

### Step 10: 数据泄漏检查与验证

**目标**：确保没有数据泄漏，预处理正确

#### 检查清单

- [ ] **数据泄漏检查**
  - [ ] 目标变量未用于特征工程
  - [ ] 未使用未来信息（时间序列）
  - [ ] 测试集统计量未泄漏到训练集

- [ ] **预处理验证**
  - [ ] 缺失值已全部处理
  - [ ] 分类特征已全部编码
  - [ ] 数值特征已缩放（如算法需要）
  - [ ] 数据形状符合预期

- [ ] **代码质量**
  - [ ] Pipeline可复现
  - [ ] 参数已记录
  - [ ] 测试集与训练集使用相同转换

#### 验证代码

```python
# 1. 检查缺失值
assert df.isnull().sum().sum() == 0, "仍有缺失值！"

# 2. 检查数据类型
object_cols = df.select_dtypes(include=['object']).columns
assert len(object_cols) == 0, f"仍有未编码的分类列: {object_cols.tolist()}"

# 3. 检查数据范围（如果做了缩放）
if scaled:
    print(f"数值列均值: {df[numeric_cols].mean().mean():.2f}")
    print(f"数值列标准差: {df[numeric_cols].std().mean():.2f}")

# 4. 记录预处理步骤
preprocessing_log = {
    'missing_strategy': 'median',
    'outlier_method': 'IQR',
    'encoding': 'onehot',
    'scaling': 'standard',
    'feature_engineering': ['datetime', 'interaction'],
    'final_shape': df.shape
}

print("✅ 预处理验证通过！")
print(preprocessing_log)
```

---

## 3. 可视化决策树

### 3.1 完整流程图（ASCII艺术）

```
                          [原始数据]
                              │
                              ↓
                   ┌──────────────────────┐
                   │  Step 1: 数据概况扫描  │
                   └──────────────────────┘
                              │
                              ↓
                   ┌──────────────────────┐
                   │  Step 2: 缺失值处理   │⭐
                   └──────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
         缺失率>50%?                  缺失率<5%?
                │                           │
                ↓                           ↓
           【删除列】                   【删除行】
                                            │
                                            ↓
                                    判断缺失机制
                                            │
                        ┌───────────────────┼───────────────────┐
                        │                   │                   │
                      MCAR                 MAR                 MNAR
                        │                   │                   │
                        ↓                   ↓                   ↓
                  【均值/中位数】        【KNN填充】        【特殊类别】
                        │                   │                   │
                        └───────────────────┴───────────────────┘
                                            │
                                            ↓
                                 ┌──────────────────────┐
                                 │  Step 3: 异常值处理   │⭐
                                 └──────────────────────┘
                                            │
                                ┌───────────┴───────────┐
                                │                       │
                          选择检测方法            判断异常值性质
                                │                       │
                        ┌───────┴───────┐       ┌───────┴───────┐
                        │               │       │               │
                      IQR        Isolation   数据错误       真实极值
                                 Forest
                        │               │       │               │
                        └───────┬───────┘       ↓               ↓
                                │           【删除】    ┌───────┴───────┐
                                │                       │               │
                                │                  线性模型        树模型
                                │                       │               │
                                │                       ↓               ↓
                                │               【鲁棒标准化】     【保留】
                                │
                                ↓
                     ┌──────────────────────┐
                     │  Step 4: 重复值检查   │
                     └──────────────────────┘
                                │
                                ↓
                        【删除重复行】
                                │
                                ↓
                     ┌──────────────────────┐
                     │  Step 5: 特征类型分析 │
                     └──────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    │                       │
               数值特征                分类特征
                    │                       │
            ┌───────┴───────┐               ↓
            │               │         检查基数
        正态分布         右偏分布              │
            │               │         ┌───────┴───────┐
            ↓               ↓         │               │
        【保留】      【log变换】   基数<10        基数>10
                                      │               │
                                      ↓               ↓
                           ┌──────────────────────┐
                           │  Step 6: 特征编码    │⭐
                           └──────────────────────┘
                                      │
                        ┌─────────────┴─────────────┐
                        │                           │
                  【One-Hot编码】            【Target编码】
                        │                           │
                        └─────────────┬─────────────┘
                                      │
                                      ↓
                           ┌──────────────────────┐
                           │  Step 7: 特征缩放    │
                           └──────────────────────┘
                                      │
                        ┌─────────────┴─────────────┐
                        │                           │
                   线性模型/SVM                树模型
                        │                           │
                        ↓                           ↓
               【StandardScaler】              【不需要缩放】
                        │
                        └─────────────┬─────────────┘
                                      │
                                      ↓
                           ┌──────────────────────┐
                           │  Step 8: 特征工程    │⭐
                           └──────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
              时间特征提取      交互特征创建      缺失值标记
                    │                 │                 │
                    └─────────────────┴─────────────────┘
                                      │
                                      ↓
                           ┌──────────────────────┐
                           │  Step 9: 数据平衡    │
                           └──────────────────────┘
                                      │
                        ┌─────────────┴─────────────┐
                        │                           │
                   平衡数据                     不平衡
                        │                           │
                        ↓                           ↓
                   【无需处理】                【SMOTE】
                        │                           │
                        └─────────────┬─────────────┘
                                      │
                                      ↓
                           ┌──────────────────────┐
                           │  Step 10: 验证检查   │
                           └──────────────────────┘
                                      │
                                      ↓
                               【预处理完成】
                                      │
                                      ↓
                              [进入建模阶段]
```

### 3.2 快速路径（15分钟）

如果时间紧急，可以只执行关键步骤：

```
[原始数据]
    ↓
Step 2: 缺失值（简单填充）
    ↓
Step 3: 异常值（快速IQR）
    ↓
Step 6: 特征编码
    ↓
Step 7: 特征缩放
    ↓
[Baseline模型]
```

参考文档：[preprocessing_quick_reference.md](preprocessing_quick_reference.md)

---

## 4. 实战案例演示

### 案例1：客户流失预测（完整流程）

**背景**：
- Telco公司客户流失预测
- 7043行 × 21列
- 目标：预测客户是否流失（Churn: Yes/No）

**完整决策过程**：

```
Step 1: 数据概况
├─ 形状: 7043 × 21
├─ 缺失值: TotalCharges列有11个(0.16%)
├─ 分类特征: 16个
├─ 数值特征: 3个
└─ 目标变量: 流失率26.5%（轻度不平衡）

Step 2: 缺失值处理
├─ 缺失率0.16% < 5% → 【删除行】
├─ 删除11行
└─ 剩余7032行

Step 3: 异常值检查
├─ 检查MonthlyCharges、TotalCharges
├─ IQR检测：无明显异常值
└─ 【保留所有数据】

Step 4: 重复值
├─ 完全重复: 0行
└─ 无需处理

Step 5: 特征类型
├─ 数值特征（3个）: tenure, MonthlyCharges, TotalCharges
│   └─ 分布检查: 正态分布 → 【保留】
│
└─ 分类特征（16个）: gender, Partner, InternetService等
    └─ 基数检查: 全部<10 → 【One-Hot编码】

Step 6: 特征编码
├─ 16个分类特征 → One-Hot编码
├─ drop_first=True（避免多重共线性）
└─ 特征数: 21 → 47

Step 7: 特征缩放
├─ 算法选择: Logistic Regression → 需要标准化
├─ 数值特征: tenure, MonthlyCharges, TotalCharges
└─ 方法: StandardScaler

Step 8: 特征工程
├─ 交互特征: tenure × MonthlyCharges = CustomerValue
├─ 缺失值标记: 无（已删除）
└─ 特征数: 47 → 48

Step 9: 数据平衡
├─ 流失率: 26.5% → ratio = 0.36（中度不平衡）
└─ 方法: class_weight='balanced'

Step 10: 验证
├─ 缺失值: 0 ✓
├─ 分类特征: 全部编码 ✓
├─ 数值特征: 已标准化 ✓
└─ 最终形状: 7032 × 48 ✓
```

**代码示例**：见 [preprocessing_quick_reference.md#示例1](preprocessing_quick_reference.md#示例1客户流失预测15分钟完整流程)

---

### 案例2：房价预测（回归问题）

**决策要点**：
- 缺失值>50%的列 → 删除
- SalePrice右偏 → log变换
- 异常值多 → IQR截断
- 树模型 → Label编码，不需要缩放

---

## 5. 常见决策路径

### 路径1：简单分类问题

```
缺失值(中位数) → 编码(One-Hot) → 缩放(Standard) → 建模
```

### 路径2：回归问题（有异常值）

```
缺失值(中位数) → 异常值(截断) → 目标变量(log) → 缩放(Robust) → 建模
```

### 路径3：高维树模型

```
缺失值(删除列>50%) → 编码(Target) → 特征选择 → 建模（不缩放）
```

### 路径4：不平衡分类

```
缺失值 → 编码 → 缩放 → 数据平衡(SMOTE) → 建模
```

---

## 📚 延伸阅读

### 深入学习特定主题

- [missing_values_strategies.md](missing_values_strategies.md) - 缺失值深入
- [outlier_detection_methods.md](outlier_detection_methods.md) - 异常值深入
- [feature_engineering_cookbook.md](feature_engineering_cookbook.md) - 特征工程深入

### 快速使用

- [preprocessing_quick_reference.md](preprocessing_quick_reference.md) - 15分钟速查
- [preprocessing_checklist.md](preprocessing_checklist.md) - 检查清单

---

**最后更新**：2024年11月
**预计使用时间**：1-2小时
**后续文档**：[preprocessing_checklist.md](preprocessing_checklist.md)
